sudokuOCR.cpp
Put the image in sudokuImg folder. It will create a txt file and save it in sudokuTxt folder with the coordinates and the value [x y k].

sudokuSolver.cpp
It reads all the txt file from sudokuTxt and generates a schema for each one.
Than it resolve each sudoku.
